# medical-diagnosis-binary-classifier
This project implements a machine learning based binary classification model to predict the presence of a disease using patient clinical features. The model is trained on labeled data and used to determine whether masked patient values indicate disease or not.
